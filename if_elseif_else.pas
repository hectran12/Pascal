program if_elseif_else;
const
    a = 30;
    b = 20;
begin
    
    if (a > b) then begin
        write('A lớn hơn B');
    end
    else if (a = b) then begin
        write('A bằng B');
    end
    else
        write('A nhỏ hơn B')

end.
